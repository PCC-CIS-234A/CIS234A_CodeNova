<%- include('partials/header', { title }) %>

<div class="home-box">
  <%- include('partials/flash') %>

  <% if (currentUser) { %>
    <h1>Welcome, <%= currentUser.first_name %>!</h1>
    <p>You are logged in.</p>
    <% /* ----- Saul's / Noah's code: role-based action links ----- */ %>
    <% const _role = String(currentUser.role || '').trim().toLowerCase(); %>
    <% if (_role === 'manager' || _role === 'staff') { %>
    <div class="home-actions">
      <% if (_role === 'manager') { %>
      <a href="/sendNotification" class="btn btn-link-action">Send Notification</a>
      <% } %>
      <a href="/notifications/log" class="btn btn-link-action btn-secondary">Notification Log</a>
    </div>
    <% } %>
    <% /* ----- end Saul's / Noah's code ----- */ %>
    <div class="home-actions">
      <a href="/account" class="btn btn-link-action btn-secondary">Edit Account</a>
    </div>
  <% } else { %>
    <h1>Welcome!</h1>
    <p>Please log in or create an account to continue.</p>

    <div class="home-actions">
      <a href="/login" class="btn btn-link-action">Login</a>
      <a href="/signup" class="btn btn-link-action btn-secondary">Create Account</a>
      <% /* ----- Saul's code ----- */ %>
      <% if (typeof devBypassAvailable !== 'undefined' && devBypassAvailable) { %>
      <a href="/dev-bypass" class="btn btn-link-action btn-secondary">Dev Bypass</a>
      <% } %>
      <% /* ----- end Saul's code ----- */ %>
    </div>
  <% } %>
</div>

<%- include('partials/footer') %>
